import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    width: "100%",
    borderRadius: 12,
    height: 50
  },
  label: {
    fontSize: 16,
    fontFamily: "Cairo",
    color: "white",
  },
});

export default styles;
